<!-- ============================================================= -->
<!-- WARNING: DERIVED ARTIFACT — DO NOT EDIT BY HAND               -->
<!-- Generated by json-hbars-transform from the LeopoldLoops rows  -->
<!-- in effortless-rulebook.json. Edit the rulebook + rerun        -->
<!-- `effortless build`. A loop's Status (done|next|planned|backlog)-->
<!-- decides where it lands below (via the IsDone/IsNext/IsPlanned/ -->
<!-- IsBacklog derived flags). Template lives at plan/leopold-plan.hbs.-->
<!-- ============================================================= -->
# Leopold Looping Plan — Causal Autoimmune Architecture Platform  *(derived)*

> **Demonstration of inference structure, not validated clinical decision support.**
>
> **This file is generated.** Source of truth = the `LeopoldLoops` rows in
> `effortless-rulebook/effortless-rulebook.json`. Change a loop's `Status` there and rerun
> `effortless build`. "Next + remaining only": the current loop is shown in full, planned/backlog are
> listed compactly, and completed loops collapse to a one-line ledger.

## The keystone (never lose sight of this)

Everything load-bearing chains up to one derived boolean:
**`IndividualPredictions.IsClinicallyActionable`** — TRUE only when a prediction is causally grounded,
well calibrated, not spurious, and ancestry-generalizable. It is **computed**, never entered.

## The loop (run exactly this every session)

1. **Look** at the current app/model/goal. 2. **Change the rulebook** (one coherent rule-change).
3. **`effortless build`**. 4. **Commit** (message = the rule that changed). 5. **Update the app** to
surface the new rule. 6. **Update the rulebook** `LeopoldLoops` rows (tick done, add follow-ups).
7. **Anti-hallucination sweep.** 8. **Commit again** (state summary). 9. **New agent**, back to step 1.

> Verification oracle every loop: `bootstrap/mock-data/scenarios.md` — {A,G} -> TRUE; {B,C,D,E,F} -> FALSE,
> each on its own gate, or the oracle is deliberately and visibly updated as part of the rule-change.

---

## Current loop (NEXT)
{{#each handlebars.LeopoldLoops.data}}{{#if IsNext}}

### Loop {{LoopNumber}} — {{Title}}

{{Goal}}

- **Rule commit:** {{RuleCommitMsg}}
- **State commit:** {{StateCommitMsg}}
{{/if}}{{/each}}

## Remaining (planned)
{{#each handlebars.LeopoldLoops.data}}{{#if IsPlanned}}
- **Loop {{LoopNumber}} — {{Title}}** — {{Goal}}
{{/if}}{{/each}}

## Backlog (promote when it earns its place)
{{#each handlebars.LeopoldLoops.data}}{{#if IsBacklog}}
- **Loop {{LoopNumber}} — {{Title}}** — {{Goal}}
{{/if}}{{/each}}

> Promotion rule: a backlog item must be traceable to `problem-to-solve.md` **or** be load-bearing
> toward the keystone. If neither, don't build it.

---

## Done ledger
{{#each handlebars.LeopoldLoops.data}}{{#if IsDone}}
- **Loop {{LoopNumber}} — {{Title}}** — {{StateCommitMsg}}
{{/if}}{{/each}}
