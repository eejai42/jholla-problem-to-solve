# Leopold Looping Plan — Causal Autoimmune Architecture Platform

> **Demonstration of inference structure, not validated clinical decision support.**

This file is the ordered list of Leopold loops that build this platform up, **commit by commit**.
Each loop runs in its own fresh agent session, does one coherent rule-change, and commits twice.
Keep this file updated every loop (loop step 6).

## The keystone (never lose sight of this)

Everything load-bearing chains up to one derived boolean:
**`IndividualPredictions.IsClinicallyActionable`** — TRUE only when a prediction is causally grounded,
well calibrated, not spurious, and ancestry-generalizable. It is **computed**, never entered. The full
derivation is in `bootstrap/derivation-spec.md`; the dependency tree bottoms out only in raw observations.

## The loop (run exactly this every session)

1. **Look** at the current admin app / model / goal (read this file + `git log` + run the app if present).
2. **Change the rulebook** (`effortless-rulebook/effortless-rulebook.json`) — one coherent rule-change.
3. **`effortless build`** (check `git status` first; ask before building on a dirty tree).
4. **Commit** — message describes *the rule that changed* (e.g. `rule: severity prediction now derives from SLEDAI evidence`).
5. **Update the app** to surface that new rule in the admin portal (the intake → diagnosis UX).
6. **Update this file** — tick the loop done, add any newly discovered follow-up loops.
7. **Anti-hallucination sweep:** walk the rulebook; remove anything that is **BOTH**
   (a) not in `bootstrap/problem-to-solve.md` **AND** (b) not load-bearing toward `IsClinicallyActionable`.
8. **Commit again** — message summarizes the current state.
9. **Start a new agent** and go back to step 1.

> Verification oracle for every loop: `bootstrap/mock-data/scenarios.md`. After any change, the seven
> patients (A,G → TRUE; B,C,D,E,F → FALSE, each on its own gate) must still resolve correctly, or the
> oracle must be deliberately and visibly updated as part of the rule-change.

## Stack (decided)

- **Substrate:** local Postgres, DB `causal_autoimmune`, regenerated by `postgres/init-db.sh` on every build. **NO migrations.**
- **App (Loop 1+):** Express API (reads `vw_*`, writes base tables) + Vite SPA. Primary screen = patient
  intake → **full prediction panel** (keystone boolean + tier + derived onset/severity/response/adverse +
  the four-gate breakdown). Disclaimer visible in the UI.
- **Realism:** real loci/HLA/drugs + literature thresholds (`bootstrap/clinical-reference.md`).

---

## Loops

### ✅ Loop 0 — Solve-by-inference rulebook (DONE this session)
- Converted the 7 hand-entered "answer" fields to **derived** fields; added 4 tables
  (`EvidenceItems`, `CohortReplications`, `NegativeControlTests`, `CalibrationBins`).
- Wired the full DAG from raw observations → `IsClinicallyActionable`.
- Seeded 7 named patients; each fails exactly one gate (+ two positives).
- Postgres builds; keystone verified: `pred-a,pred-g = TRUE`; `pred-b..f = FALSE`.
- **Commits:** `rule: keystone IsClinicallyActionable now derived from observations`; `state: Loop 0 — Postgres solve-by-inference verified`.

### ▶ Loop 0.5 — Test Harness First (the red contract for the whole app) (NEXT)
**Inserted before Loop 1 deliberately: red-green test-first.** Before the intake app surfaces a
single prediction, the admin app ships with a **witnessed inference test harness** that asserts the
*entire* DAG — every derived node, from raw observations up to the keystone — for all seven oracle
patients. Each test carries three things:
1. **Raw test facts** — the leaf observations the node bottoms out in (allele freq, ZStat inputs,
   replication signs/p-values/ancestry, permutation effect sizes, calibration coverage, cryptic flag).
2. **Witnessed line of reasoning** — the expert-verifiable derivation trace from
   `bootstrap/derivation-spec.md` §3 (e.g. "conf 0.758 ≥ 0.7 ∧ falsifiable ∧ ¬spurious ⇒ node").
3. **Expected derived value** — the ground truth captured live from `vw_*` (committed as the oracle).

These tests are **RED on arrival** because the app's prediction surface (the `/api/*` endpoints + the
panel the UI renders) does not exist yet. They are **load-bearing**: Loops 1–5 turn them green by
actually building the app surface that serves each node. A node is "done in the UI" only when its
witnessed test passes against the real running server — not when it merely exists in Postgres.
- Scaffold the Express + Vite admin app skeleton now (server reads `vw_*`, writes base tables; Vite SPA).
- Harness lives **inside** the app (`admin-app/tests/`): every DAG node × 7 patients, levels L0→L7,
  each with its raw facts + reasoning witness + expected value.
- The harness hits the app's own API (not the DB directly) so it proves the *app* surfaces the
  inference faithfully — that's what makes it load-bearing on the app, not just on Postgres.
- **Commit (rule):** none — "app-only loop, test-harness-first, no rule change".
- **Commit (state):** "state: Loop 0.5 — red witnessed-inference harness is the app's contract".

### ▶ Loop 1 — Intake app skeleton
- App skeleton already scaffolded in Loop 0.5; this loop **turns the keystone-level red tests green**
  by wiring the cohort + prediction-panel endpoints the harness asserts against.
- Read-only cohort list from `vw_individual_predictions` + a patient-detail **full prediction panel**:
  keystone boolean, `PatientStratificationTier`, `PredictedValue`, `CalibratedUncertainty`, and the four
  gate booleans with pass/fail styling. No editing yet.
- **Commit (rule):** none to rulebook this loop — note in message "app-only loop, no rule change".
- **Commit (state):** "state: Loop 1 — read-only intake app surfaces the keystone".

### ▶ Loop 2 — Patient intake (facts in)
- Add a form that writes a new `Individuals` row + its child observation rows (variants, evidence items,
  replications, controls, calibration bins) to the **base tables**, then re-reads the derived panel.
- This is the demo payoff: enter facts → derived diagnosis.
- Likely no rule change; if intake needs a field, add it to the rulebook (step 2) and rebuild.
- **Commits:** rule (if any) + "state: Loop 2 — facts-in → derived diagnosis works end to end".

### ▶ Loop 3 — Gate explainability in the UI
- For a selected patient, show *why* each gate passed/failed by walking one level down (e.g. calibration:
  show bin coverage; transport: show cross-ancestry replication count). Consider installing the
  **explainer-DAG** transpiler (`effortless-explainer-dag`) so any field is click-to-trace.
- **Commits:** rule (explainer install) + state.

### ▶ Loop 4 — Second prediction type (severity)
- Today only onset risk drives `PredictedValue`. Add a derived **severity** prediction grounded in
  `ClinicalPhenotypes.SeverityScore` (SLEDAI/DAS28-aligned) as real evidence, wired into a per-type
  prediction. This pulls one currently-context table onto the load-bearing path *intentionally*.
- Update the oracle (`scenarios.md`) to cover the new prediction type.
- **Commits:** "rule: severity prediction derived from clinical-activity evidence" + state.

### ▶ Loop 5 — Treatment-response prediction
- Derive a treatment-response prediction from `Treatments` (rituximab/anifrolumab partial-response reality)
  + mechanism match. Surface in the panel.
- **Commits:** rule + state.

### Backlog / candidate loops (promote as needed)
- Adverse-effect prediction wired to observed treatment adverse-event rows.
- Bring one sparse modality (methylome / Hi-C) onto the evidence path as real cross-modality support.
- Epistasis: let `EpistaticInteractions` raise/lower `CausalConfidence` when load-bearing.
- Cohort-level equity report: calibration & actionability rates by ancestry (ancestry-equity dashboard).
- Bitemporal: capture retroactive corrections to observations as rows (ERB bitemporality).

> When promoting a backlog item, first check it against step 7's rule: it must be traceable to
> `problem-to-solve.md` **or** be load-bearing toward the keystone. If neither, don't build it.

## Anti-hallucination ledger (step 7 each loop)

Record here anything pruned and why, so the model stays honest.

- _Loop 0:_ nothing pruned (this was the constructive loop). Tables kept as **deliberate sparse context**
  (off the keystone path, present for breadth): `EnvironmentalExposures`, `Treatments`,
  `ClinicalPhenotypes`, `EpistaticInteractions`, `CounterfactualTrajectories`, and the
  methylome / Hi-C / extra modality rows. These are candidates to either (a) become load-bearing in a
  future loop or (b) be pruned if they never earn their place.
